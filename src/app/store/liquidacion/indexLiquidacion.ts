import { LiquidacionEffects } from './liquidacion.effects';
import {
  liquidacionFeatureKey,
  liquidacionReducer,
} from './liquidacion.reducer';
export const liquidacionRoot = {
  LiquidacionEffects,
  liquidacionFeatureKey,
  liquidacionReducer,
};
